# ── Core ─────────────────────────────────────────
# Groq speaks the OpenAI chat-completions protocol, so the openai client drives it.
groq
openai
yt-dlp
aiohttp
requests
Pillow>=10.0.0

# ── Voice ────────────────────────────────────────
# edge-tts: free Microsoft neural TTS (no API key) — the default engine.
edge-tts

# Coqui local TTS (optional; only for VOICE_PROVIDER=coqui). Heavy (~2 GB with
# torch). Uncomment only if you want fully-local voice cloning.
# TTS
# torch
# torchaudio

# ── Persistence / config ─────────────────────────
aiosqlite
python-dotenv
pydantic>=2.0.0

# ── Integrations (optional layers) ───────────────
fastapi
uvicorn
mcp
croo-sdk
boto3
